* Pass key/index as first block arg to :class and :extendv
class: |key, hsh|

* Don't extend when lambda returns nil
	class: nil -> no extend

* Allow passing options to Binding#serialize.
	serialize(.., options{:exclude => ..})